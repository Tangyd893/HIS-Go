# HIS-Go 待办清单

> 版本：v3.1 · 2026-06-03  
> **部署主文档**：[`docs/云端部署指南.md`](./云端部署指南.md)  
> 本文档由项目审查自动生成/维护，按优先级排列。

---

## 当前目标

| 目标 | 前端 | 后端 Compose |
|------|------|----------------|
| Web 管理端演示 | `frontend/admin` | `deploy/compose/demo-admin.yml` |
| 小程序患者端 | `frontend/mp-webview` + `frontend/patient` | `deploy/compose/demo-patient.yml` |

React 版在 `frontend/archive/`，本阶段不验收。

---

## P0 — 上云前 Gate（阻塞演示）

- [ ] **G-1** 本地 `make build-frontend` 成功
- [ ] **G-2** `make demo-admin` → `http://localhost/admin` 主路径跑通（登录→排班→挂号→接诊→处方→收费→发药 ≥6 步）
- [ ] **G-3** `make demo-patient` → `http://localhost/patient` 主路径跑通（登录→预约→处方→报告）
- [ ] **G-4** `deploy/config/demo.env` 已改强密码（勿用 `change_me_*`）
- [ ] **G-5** 微信开发者工具 + `mp-webview` 本地 web-view 验证
- [ ] **G-6** VPS 实测一个 Profile + HTTPS（小程序真机必须）

---

## P1 — 演示数据与联调（影响「能讲清楚」）

### 后端 seed（`backend/sql/seed_data.sql`）

- [ ] **S-1** 补充患者端演示数据：处方、检查报告、随访计划（当前仅有号源/药品，列表页可能为空）
- [ ] **S-2** 确认 `demo-patient` 账号可登录且 JWT 角色满足患者端路由守卫
- [ ] **S-3** 跑通 seed 后执行一次主路径手工验证，记录缺失表/字段

### 管理端 `frontend/admin`

- [ ] **A-1** 主路径各页 API 端到端验证（registration → pharmacy）
- [ ] **A-2** Dashboard 仍为静态数字 → 接 statistics 或降级说明（可选，不阻塞）

### 患者端 `frontend/patient`

- [ ] **B-1** 预约/处方/报告/随访/健康档案页面联调
- [ ] **B-2** **登录页提示错误**：当前显示 `demo-doctor`，应改为 `demo-patient / demo123`
- [ ] **B-3** Dashboard 汇总数据（可选）

### 小程序 `frontend/mp-webview`

- [ ] **B-4** 上云：`USE_CLOUD = true` + 微信业务域名

### 集成测试

- [ ] **S-4** `testing/api/demo_admin_flow_test.go`、`demo_patient_flow_test.go` 本地跑通
- [ ] **S-5** CI 增加可选 integration job（Compose 起 Profile 后跑 Demo 测试）

---

## P2 — 工程与仓库卫生（值得优化）

### 源码污染（高优先级清理）

- [ ] **E-1** 删除 `frontend/admin/src`、`frontend/patient/src` 下误生成的 `*.vue.js`、`*.js`（与 `.ts`/`.vue` 并存，非 hand-written）
- [ ] **E-2** `.gitignore` 增加：`frontend/**/src/**/*.vue.js`，并排查 vue-tsc 是否误 emit（`tsconfig` 应 `noEmit: true`）

### 文档与 README 不同步

- [ ] **E-3** **README.md** 仍以 React 为主、引用旧 `docker/` 路径 → 改为 Vue 演示 + `deploy/`
- [ ] **E-4** **docs/项目架构设计文档.md** 目录树更新为 `admin`/`patient`/`deploy/`
- [ ] **E-5** **CHANGELOG.md** 路径改为 `deploy/compose/*`
- [ ] **E-6** **deploy/nginx/nginx.conf** 注释改为 `admin`/`patient`（现为 his-web-admin）

### 脚本整理

- [ ] **E-7** `backend/run_*.bat`、`run_profile_a.bat` 移至 `scripts/windows/`，避免 backend 根目录杂乱
- [ ] **E-8** 新增 **Windows** 演示脚本：`scripts/demo-admin.ps1`、`scripts/demo-patient.ps1`（当前仅 bash，本机 PowerShell 不便）
- [ ] **E-9** 合并冗余验证脚本：`verify-demo.sh` / `demo-verify.sh` / `demo-verify-example.sh` 保留一个，Makefile 只暴露 `verify-admin` / `verify-patient`
- [ ] **E-10** `verify-demo.sh` 中小程序路径改为 `frontend/mp-webview`

### Deploy / Docker

- [ ] **E-11** `deploy/compose/*.yml` 文件头注释乱码（编码损坏）→ 重写为 UTF-8
- [ ] **E-12** 新增 `deploy/nginx/nginx.ssl.conf.example`（HTTPS 模板，云端部署指南可引用）
- [ ] **E-13** 患者端专用演示时 Nginx 根路径仍 302 到 `/admin` → 可按 Profile 提供 `nginx.patient.conf` 或文档说明
- [ ] **E-14** 新增 `backend/.dockerignore`，缩小镜像构建上下文、加快 `docker compose build`
- [ ] **E-15** 文档化 PostgreSQL 宿主机映射 **5433→5432**（避免与本机 PG 冲突）
- [ ] **E-16** `deploy/compose/stack.prod.yml` 服务名仍带 `react` 字样 → 与 Vue 构建对齐

### 包名与目录一致

- [ ] **E-17** `frontend/admin/package.json`、`patient/package.json` 的 `name` 仍为 `his-web-admin` → 可选改为 `his-admin` / `his-patient` 与目录一致

### CI

- [ ] **E-18** 确认 GHA `frontend-build` 的 `working-directory` 表达式在 GitHub 上有效；更稳妥：matrix 增加 `dir: admin|patient` 字段

### 根目录

- [ ] **E-19** 删除或移走根目录 `todo/`（已在 `.gitignore`，但占本地空间）
- [ ] **E-20** 确认 `deploy/config/demo.env` 未被 Git 跟踪（已在 `.gitignore`，提交前 `git status` 检查）

---

## P3 — 体验与后续（演示完成后）

- [ ] **F-1** 管理端 B 档页面（EMR/住院/统计等）在 Profile 未启服务时友好占位
- [ ] **F-2** 小程序原生页（脱离 web-view）性能优化
- [ ] **F-3** 真实支付 / 医保 Stub 替换
- [ ] **F-4** OpenTelemetry + Grafana
- [ ] **F-5** Tauri 桌面端（壳加载 `frontend/admin`）
- [ ] **F-6** 全量 18 服务 / K8s（见《部署运维手册》，非演示必需）

---

## 搁置

- React 前端（`frontend/archive/`）
- 全量生产 HA、等保合规上线

---

## 已完成

- [x] 删除误建 `%USERPROFILE%/` 目录（Windows 环境变量冲突）
- [x] `docker/` → `deploy/`（compose / config / nginx）
- [x] 前端目录：`admin`、`patient`、`mp-webview`；React → `archive/`
- [x] 合并文档为《云端部署指南》；删除重复演示验证类文档
- [x] Vue 生产 base、Makefile/CI、`demo-*.sh` 路径更新
- [x] `GET /api/auth/current`
- [x] seed 基础：用户/科室/患者/排班/药品（`seed_data.sql` 部分已补）

---

## 项目结构速查

```
HIS-Go/
├── backend/                 # Go 微服务 + sql/seed + migrations
├── deploy/                  # Docker 部署（见 deploy/README.md）
│   ├── compose/             # demo-admin · demo-patient · stack
│   ├── config/              # demo.env（勿提交）
│   └── nginx/
├── frontend/
│   ├── admin/               # Vue 管理端（演示）
│   ├── patient/             # Vue 患者 H5（演示）
│   ├── mp-webview/          # 微信小程序壳
│   └── archive/             # React 搁置
├── docs/                    # 见 docs/README.md
├── scripts/                 # demo-admin.sh · cloud-init.sh · verify-demo.sh
└── testing/                 # 集成测试
```

---

## 建议执行顺序

```
P2 E-1/E-3（清理 + README）→ P0 Gate → P1 seed/登录提示/联调 → P2 脚本与 Docker 优化 → P1 集成测试 → P0 G-6 上云
```
