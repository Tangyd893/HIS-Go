# HIS-Go 待办清单

> 版本：v3.9 · 2026-06-08  
> **演示主文档**：[`docs/快速启动-患者端.md`](./快速启动-患者端.md) · [`deploy/README.md`](../deploy/README.md)  
> 本文档由项目审查自动生成/维护，按优先级排列。  
> **范围说明**：**不上云**，仅本地 / 局域网 Docker Compose + 微信开发者工具演示。  
> **SonarCloud 快照**：[`docs/sonarcloud-report.md`](./sonarcloud-report.md)

---

## 当前目标

| 目标 | 前端 | 后端 Compose |
|------|------|----------------|
| Web 管理端演示 | `frontend/admin` | `deploy/compose/demo-admin.yml` |
| 小程序患者端 | `frontend/mp-webview` + `frontend/patient` | `deploy/compose/demo-patient.yml` |
| **就诊助手（RAG）** | `frontend/patient` 聊天页 | `outpatient` + `user`（科室） |

React 版在 `frontend/archive/`，本阶段不验收。

---

## 项目状态摘要（2026-06-08）

| 维度 | 状态 |
|------|------|
| **就诊助手（P0+）** | **✅ 代码完成**：知识库 (16 条目/44 chunks) + 嵌入脚本 + 后端 assistant 包 + `POST /api/outpatient/assistant/chat` + 前端聊天页 + 路由/Tab/横幅/挂号按钮；**待 Key 联调（TA-15）** |
| **本地编译/测试** | **✅ 通过**：`go vet` / `go test`（16 个 service 包）/ `go build ./cmd/...`；患者端 + 管理端 `npm run build` |
| 工程卫生（P2） | 大部分已落地；Docker Compose Dockerfile 路径已修复 (39 处)；Shell 脚本语法已修复 (30 处) |
| SonarCloud | 已刷新（2026-06-08），**458 Issue**（↑10）；质量门 **❌ ERROR**（**新增重复率 6.1% > 3%**）；全量重复率 **6.0%**；代码行 **38,897**；Bug **0**；漏洞 **1**（`testing/go.mod` 缺 `go.sum`）；覆盖率 **N/A** |
| 演示 Gate（P0） | 前端构建 ✅；Docker Compose 就绪；**待 Docker 实测（G-2/G-3）** |
| 联调（P1） | 挂号 API 已补全 (list/create/update/delete)；慢病签约已补全；seed 数据完整；**挂号单元测试 12 场景 ✅** |

---

## P0+ — 就诊助手（RAG + DeepSeek）✅ 已完成

> **目标**：用户描述症状 → 检索知识库（病症大类）→ 结合 `departments` 表本院科室 → DeepSeek 生成挂号建议 → 跳转预约。  
> **方案 A**：关键词 + SiliconFlow **BGE-M3** 语义检索双路召回，DeepSeek Chat 生成；Key 仅后端 `demo.env`。

### 知识库与嵌入（离线）

- [x] **TA-1** 创建 `backend/data/triage/knowledge.json`（16 条目：呼吸/心血管/消化/神经/泌尿/内分泌/骨骼/皮肤/眼科/耳鼻喉/口腔/妇产/儿科/精神/创伤/体检）
- [x] **TA-2** 编写 `scripts/build-triage-embeddings.py`：加载 knowledge → 切分 44 chunks → 调 SiliconFlow BGE → 输出 `embeddings.json`；无 Key 时自动占位降级
- [x] **TA-3** Makefile 增加 `make triage-embed` / `make triage-embed-check`

### 后端（`outpatient` 服务）

- [x] **TA-4** `deploy/config/demo.env.example` 增加：`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`、`SILICONFLOW_API_KEY`、`SILICONFLOW_EMBED_MODEL`、`TRIAGE_RAG_ENABLED`、`TRIAGE_TOP_K`
- [x] **TA-5** 实现 RAG 检索层 `internal/outpatient/assistant/` — `config.go` / `retriever.go`（关键词打分 + BGE 语义余弦合并 Top-K）/ `llm_client.go` / `department_resolver.go` / `service.go`
- [x] **TA-6** 实现 DeepSeek Chat 客户端（system prompt 含免责声明）
- [x] **TA-7** 运行时拉取本院科室：HTTP 调用 `his-user:8082/api/user/departments`，与知识库 `dept_types` 模糊匹配取交集
- [x] **TA-8** 新增 `POST /api/outpatient/assistant/chat`（请求：`{symptom}`；响应：advice + depts + urgency + mode + disclaimer）
- [x] **TA-9** Key 缺失或 `TRIAGE_RAG_ENABLED=false` 时降级为关键词模式，不 500
- [x] **TA-10** Gateway 已自动代理 `/api/outpatient/*`；JWT 鉴权由网关统一处理

### 患者端（`frontend/patient`）

- [x] **TA-11** 新增「就诊助手」页 `views/triage/TriageChatView.vue`：聊天 UI（用户输入 → 加载动画 → AI/Markdown 回复 → 科室按钮）
- [x] **TA-12** 路由 `/triage` + 底部 Tab「助手」(RobotOutlined) + 首页横幅入口
- [x] **TA-13** 推荐科室展示「预约科室名」按钮，跳转 `/appointment?deptId=xxx`
- [x] **TA-14** 固定展示免责声明 footer：「⚠️ 本建议仅供参考，不能替代专业医疗诊断」

### 验证与文档

- [ ] **TA-15** 本地联调：`demo.env` 填入 DeepSeek + SiliconFlow Key → 典型问句验收（如「咳嗽三天挂什么科」→ 内科）
- [ ] **TA-16** 更新 `docs/快速启动-患者端.md`：Key 配置、嵌入构建、助手演示路径
- [ ] **TA-17** （可选）`testing/api/demo_patient_assistant_test.go` 冒烟测试
- [ ] **TA-18** `internal/outpatient/assistant/` 单元测试（RAG 检索、关键词降级、科室匹配兜底）；当前 **0 测试文件**

---

## P0 — 本地演示 Gate（阻塞验收）

- [x] **G-1** 本地 `make build-frontend` 成功 ✅（管理端 + 患者端均已通过 vue-tsc + vite build）
- [ ] **G-2** `make demo-admin` → `http://localhost/admin` 主路径跑通（登录→排班→挂号→接诊→处方→收费→发药 ≥6 步）
- [ ] **G-3** `make demo-patient` → `http://localhost/patient` 主路径跑通（登录→预约→处方→报告）
- [x] **G-4** `deploy/config/demo.env` 已改强密码（MD5 随机值，非 `change_me_*`）
- [ ] **G-5** 微信开发者工具 + `mp-webview` 本地 web-view 验证（勾选「不校验合法域名」；H5 指向 `127.0.0.1:5174/patient/`）
- [ ] **G-6** （可选）局域网真机预览：同 WiFi，`index.js` 改为 PC 局域网 IP，开发者工具扫码

---

## P1 — 演示数据与联调（影响「能讲清楚」）

### 后端 seed（`backend/sql/seed_data.sql`）

- [ ] **S-2** 确认 `demo-patient` 账号可登录且 JWT 角色满足患者端路由守卫（账号已 seed，待联调）
- [ ] **S-3** 跑通 seed 后执行一次主路径手工验证，记录缺失表/字段

### 管理端 `frontend/admin`

- [x] **A-1** 挂号 API 端到端：新增 `GET /api/registration/list` + `POST/PUT/DELETE /api/user/patients`；前端 RegistrationView / PatientList 已接真实 API
- [ ] **A-2** Dashboard 仍为静态数字 → 接 statistics 或降级说明（可选，不阻塞）

### 患者端 `frontend/patient`

- [x] **B-1** 慢病签约：新增 `GET /api/outpatient/contract`；ChronicView 挂载时获取签约数据
- [ ] **B-3** Dashboard 汇总数据（可选）

### 小程序 `frontend/mp-webview`

- [ ] **B-4** 确认 `index.js` 本地 H5 地址含 `/patient/` 路径，避免 web-view 白屏（`USE_CLOUD` 保持 `false`）

### 集成测试

- [ ] **S-4** `testing/api/demo_admin_flow_test.go`、`demo_patient_flow_test.go` 本地跑通
- [ ] **S-5** CI 增加可选 integration job（Compose 起 Profile 后跑 Demo 测试）

### 已知逻辑注意点（非阻塞）

- **REG-1** 排队号 `QueueNumber = CountBySchedule + 1` 含已取消记录，取消后再挂号可能出现号段跳跃（仅影响显示）

---

## P2 — 工程与仓库卫生（值得优化）

### Deploy / Nginx

- [x] **E-13** 患者端专用 Nginx 配置：新建 `deploy/nginx/nginx.patient.conf`（根路径 302 → `/patient`，患者端优先）

### Docker Compose

- [x] **Dockerfile 路径修复**：4 个 compose 文件中 `dockerfile: ../Dockerfile` → `../deploy/Dockerfile`（39 处），修复 context 解析错误

### Shell 脚本

- [x] **语法修复**：5 个 `.sh` 文件中 `[[[` → `[[`（30 处），修复 Bash 语法错误导致脚本不可运行

### 前端

- [x] **统计页初始值**：`StatisticsView.vue` 硬编码假数据 → 初始值 `0`，由 API 填充

### SonarCloud 代码质量

> 依据 [`docs/sonarcloud-report.md`](./sonarcloud-report.md)（2026-06-08）：质量门 **❌ ERROR**（**阻塞项**：`new_duplicated_lines_density` **6.1%** > 3%）· 代码行 **38,897** · **458** 未解决 Issue（0 BLOCKER / 228 CRITICAL / 59 MAJOR / 171 MINOR）· Bug **0** · 漏洞 **1** · 重复率 **6.0%**（目标 ≤5%）· 覆盖率 **N/A**（目标 ≥80%）· 技术债务 **38h 24min**  
> 刷新命令：`python scripts/fetch-sonar.py && python scripts/gen-sonar-report.py`

- [ ] **SQ-6** 质量门从 ERROR → ✅ PASSED（优先降低**新增代码**重复率 ≤3%，其次消减存量 Issue）
- [ ] **SQ-7** 修复剩余 **1** 个漏洞：`testing/go.mod` 缺少 `go.sum`（`text:S8566`，依赖版本不可预测）
- [ ] **SQ-8** 上报 Go 测试覆盖率（消除 N/A，朝 ≥80% 目标推进）
- [ ] **SQ-9** 降低重复率至 ≤5%（当前 6.0%）
- [ ] **SQ-C1** 🟠 修复 `go:S1186` 空函数（138 处）：为 stub 添加 `// TODO: implement` 或实现
- [ ] **SQ-C2** 🟠 修复 `go:S1192` Go 字符串重复（41 处；含 `auth_service.go` / `registration_repo.go`）
- [ ] **SQ-C3** 🟠 修复 `plsql:S1192` SQL 字符串重复（42 处；`seed_data.sql` 为主）
- [ ] **SQ-C4** 🟠 修复 `go:S3776` 认知复杂度（7 处；**assistant 包** `service.go`/`retriever.go`/`department_resolver.go`）
- [ ] **SQ-M1** 🔵 修复 `go:S100` 简化写法（121 处，Sonar MINOR 最高频）
- [ ] **SQ-M2** 🟡 修复 `typescript:S6853` TS 问题（14 处）
- [ ] **SQ-M3** 🟡 修复 `powershelldre:S8626` PowerShell 脚本问题（26 处，新增高频）

---

## P3 — 体验与后续（演示完成后）

- [ ] **F-1** 管理端 B 档页面（EMR/住院/统计等）在 Profile 未启服务时友好占位
- [ ] **F-2** 小程序原生页（脱离 web-view）性能优化
- [ ] **F-3** 真实支付 / 医保 Stub 替换
- [ ] **F-4** OpenTelemetry + Grafana
- [ ] **F-5** Tauri 桌面端（壳加载 `frontend/admin`）
- [ ] **F-6** 全量 18 服务 / K8s（见《部署运维手册》，非演示必需）

---

## 搁置

- React 前端（`frontend/archive/`）
- **上云部署**（VPS、HTTPS、备案域名、微信业务域名、`USE_CLOUD`、公网小程序真机）
- 全量生产 HA、等保合规上线
- 《云端部署指南》保留备查，**非当前验收路径**

---

## 已完成

- [x] 删除误建 `%USERPROFILE%/` 目录（Windows 环境变量冲突）
- [x] `docker/` → `deploy/`（compose / config / nginx）
- [x] 前端目录：`admin`、`patient`、`mp-webview`；React → `archive/`
- [x] 合并文档为《云端部署指南》；删除重复演示验证类文档
- [x] Vue 生产 base、Makefile/CI、`demo-*.sh` 路径更新
- [x] `GET /api/auth/current`
- [x] seed 基础：用户/科室/患者/排班/药品（`seed_data.sql`）
- [x] **S-1** 患者端演示数据：处方、检查报告、随访计划（`seed_data.sql` L120+）
- [x] **B-2** 患者端登录页提示改为 `demo-patient / demo123`（`LoginView.vue`）
- [x] **E-1** 清理 `*.vue.js` 污染（当前 `frontend/**/src` 无此类文件）
- [x] **E-2** `.gitignore` 增加 `frontend/**/*.vue.js`；`tsconfig` 已 `noEmit: true`
- [x] **E-3** README 以 Vue + `deploy/` 为主（React 仅归档对比表）
- [x] **E-4** `docs/项目架构设计文档.md` 目录树含 `admin`/`patient`/`deploy/`
- [x] **E-5** CHANGELOG 路径为 `deploy/compose/*`
- [x] **E-6** `deploy/nginx/nginx.conf` 注释为 admin/patient
- [x] **E-7** `backend/run_*.bat` 已移出 backend 根目录（脚本在 `scripts/`）
- [x] **E-8** Windows 演示脚本：`scripts/demo-admin.ps1`、`scripts/demo-patient.ps1`
- [x] **E-9** 验证脚本合并为 `scripts/verify-demo.sh`；Makefile 暴露 `verify-admin` / `verify-patient`
- [x] **E-10** `verify-demo.sh` 小程序路径为 `frontend/mp-webview`
- [x] **E-11** `deploy/compose/*.yml` 文件头 UTF-8 英文注释
- [x] **E-12** `deploy/nginx/nginx.ssl.conf.example` HTTPS 模板
- [x] **E-13** `deploy/nginx/nginx.patient.conf` 患者端专用 Nginx 配置
- [x] **E-14** `backend/.dockerignore`
- [x] **E-15** PostgreSQL 宿主机 **5433→5432** 已文档化（README、`deploy/README.md`）
- [x] **E-16** `stack.prod.yml` 服务名为 `build-admin`/`build-patient`（无 react 字样）
- [x] **E-17** `package.json` name 为 `his-admin` / `his-patient`
- [x] **E-18** GHA `frontend-build` matrix `app: [admin, patient]`
- [x] **E-19** 根目录 `todo/` 已在 `.gitignore`
- [x] **E-20** `deploy/config/demo.env` 未被 Git 跟踪
- [x] **SQ-1** `sonar-project.properties`（Key: `Tangyd893_HIS-Go`）
- [x] **SQ-2** GHA `SONAR_TOKEN` + 首次扫描已产出报告
- [x] **SQ-3** GHA `sonarcloud` job（`SonarSource/sonarqube-scan-action@v6`）
- [x] **SQ-4** Go 测试 `-coverprofile=coverage.out` 并配置 `sonar.go.coverage.reportPaths`
- [x] **SQ-5** SonarCloud 分析持续更新（458 Issue，见 2026-06-08 报告）
- [x] **SQ-B1** 4 个 BLOCKER 已清零（`seed_data.sql` bcrypt `secrets:S8215` 已通过 NOSONAR/排除策略解决）
- [x] **TA-1~TA-14** 就诊助手全栈实现（知识库 + 嵌入 + 后端 + 前端）
- [x] **G-1** `make build-frontend` 成功
- [x] **G-4** demo.env 强密码
- [x] **A-1** 挂号 API 端到端（registration list + patient CRUD）
- [x] **B-1** 慢病签约获取
- [x] Docker Compose Dockerfile 路径修复（4 文件 × 39 处）
- [x] Shell 脚本 `[[[` → `[[` 语法修复（5 文件 × 30 处）
- [x] StatisticsView 假数据清零
- [x] **V-1** 本地后端验证：`go vet` / `go test -count=1 ./...` / `go build ./cmd/...` 全部通过
- [x] **V-2** 本地前端验证：`frontend/patient` + `frontend/admin` 独立 `npm run build` 通过
- [x] **V-3** SonarCloud 报告刷新脚本 `scripts/fetch-sonar.py` + `docs/sonarcloud-report.md` 更新（2026-06-08 首次）
- [x] **V-4** SonarCloud 报告再次刷新：458 Issue · 重复率 6.0% · 技术债务 38h 24min（2026-06-08）

---

## 项目结构速查

```
HIS-Go/
├── backend/                 # Go 微服务 + sql/seed + migrations
│   └── data/triage/         # 就诊助手知识库（knowledge · chunks · embeddings）
├── deploy/                  # Docker 部署（见 deploy/README.md）
│   ├── compose/             # demo-admin · demo-patient · stack
│   ├── config/              # demo.env（勿提交）
│   └── nginx/
│       ├── nginx.conf           # 管理端优先（根路径 → /admin）
│       └── nginx.patient.conf   # 患者端优先（根路径 → /patient）
├── frontend/
│   ├── admin/               # Vue 管理端（演示）
│   │   └── src/views/       # 18 页面（registration→pharmacy 主路径完整）
│   ├── patient/             # Vue 患者 H5（演示）
│   │   └── src/views/       # 10 页面（含新增 triage 就诊助手）
│   ├── mp-webview/          # 微信小程序壳
│   └── archive/             # React 搁置
├── docs/                    # 见 docs/README.md
├── scripts/                 # demo-admin.sh · demo-patient.sh · verify-demo.sh · fetch-sonar.py · gen-sonar-report.py · build-triage-embeddings.py
└── testing/                 # 集成测试
```

---

## 建议执行顺序

```
P0 本地 Gate（G-2/G-3 Docker 实测 → G-5 微信开发者工具）
  ↓
P1 联调（S-2/S-3 手工验证主路径 → S-4 集成测试 → B-4 mp-webview 路径确认）
  ↓
P2 SonarCloud 消减（SQ-7 go.sum → 新增重复率 ≤3% → SQ-C4 assistant 复杂度 → SQ-C1 → SQ-M1 → SQ-6/8/9）
并行：TA-15 就诊助手 Key 联调 · TA-18 assistant 单元测试
```